[pytest]
minversion = 8.0
testpaths = tests
python_files = test_*.py
python_classes = *Tests
addopts = -ra --strict-markers
markers =
    unit: fast tests without Streamlit AppTest or real infrastructure
    app: Streamlit AppTest browser-independent page tests
    integration: opt-in tests that access a real external service
