# AI Test Assistant - 产品需求说明书（PRD）

---

## 1. 项目解决的核心问题

### 1.1 行业痛点分析

| 痛点 | 现状 | 影响 |
|------|------|------|
| **测试点提取效率低** | 测试工程师手动阅读PRD/需求文档，逐条分析提取测试点 | 耗时平均2-4小时/需求，易遗漏边界场景和异常分支 |
| **测试用例编写繁琐** | 手动编写pytest测试用例骨架，重复编写assert、setup/teardown | 每个测试点耗时10-15分钟，代码冗余度高 |
| **日志分析难度大** | 面对海量日志，依赖人工经验定位异常和根因 | 线上问题排查平均耗时1-2小时，新手难以独立完成 |
| **知识沉淀不足** | 测试经验分散在个人脑中，无法有效复用 | 人员变动导致知识流失，新员工上手慢 |

### 1.2 核心价值主张

**「让AI成为测试工程师的智能助手，将重复劳动交给机器，让人专注于创造性测试设计」**

通过大模型能力，实现：
- **效率提升**：测试点生成时间从小时级缩短到分钟级
- **质量保障**：覆盖更多边界场景和异常分支，减少遗漏
- **经验复用**：将资深工程师的分析逻辑沉淀为AI模型能力

---

## 2. 目标用户和使用场景

### 2.1 目标用户画像

| 用户角色 | 核心需求 | 痛点 |
|---------|---------|------|
| **初级测试工程师** | 快速上手测试工作，学习测试方法 | 缺乏经验，测试点设计不全面 |
| **中级测试工程师** | 提升测试效率，减少重复劳动 | 编写测试用例耗时，日志分析困难 |
| **测试开发工程师** | 快速生成测试代码骨架，提升代码质量 | 重复编写相似的测试代码结构 |
| **开发工程师** | 快速编写单元测试，提高代码覆盖率 | 不熟悉测试框架，编写测试耗时 |

### 2.2 典型使用场景

#### 场景一：需求分析阶段 - 测试点生成
- **用户**：测试工程师小王
- **场景**：产品经理刚提交了新功能PRD文档，小王需要在1小时内完成测试点分析
- **操作流程**：
  1. 小王启动AI Test Assistant（Streamlit应用）
  2. 上传PRD文档（或粘贴需求描述）
  3. 可选：上传/选择本地Bug经验知识库
  4. 点击「生成测试点」按钮
  5. AI返回结构化测试点文档（含功能测试、边界测试、异常测试）
  6. 小王审核并补充个性化测试点

#### 场景二：测试执行阶段 - 测试用例生成
- **用户**：测试开发工程师小李
- **场景**：根据测试点文档，需要编写100个pytest自动化测试用例
- **操作流程**：
  1. 小李上传测试点文档
  2. 输入目标项目配置（模块名称、类名、方法名）
  3. AI生成完整的pytest测试用例代码
  4. 小李补充具体的断言逻辑和测试数据
  5. 运行测试验证

#### 场景三：问题排查阶段 - 日志分析
- **用户**：测试工程师小张
- **场景**：线上系统出现异常，需要分析日志定位问题
- **操作流程**：
  1. 小张上传日志文件（或粘贴日志片段）
  2. 系统自动预过滤ERROR/Exception等异常日志
  3. AI分析过滤后的日志，定位可能根因
  4. 给出排查建议和修复方案
  5. 小张按照建议进行验证

---

## 3. MVP版本需求

### 3.1 核心需求清单

| 需求编号 | 需求描述 | 优先级 | 验收标准 |
|---------|---------|-------|---------|
| MVP-001 | 用户可输入文本形式的需求描述 | P0 | 支持文本框输入，最大支持10000字符 |
| MVP-002 | 用户可上传PRD文档（.md/.txt格式） | P0 | 支持文件上传，最大5MB |
| MVP-003 | AI根据需求生成结构化测试点 | P0 | 输出包含功能测试、边界测试、异常测试的Markdown文档 |
| MVP-004 | 用户可导出测试点文档 | P0 | 支持导出为Markdown文件 |
| MVP-005 | AI根据测试点生成pytest测试用例骨架 | P0 | 输出包含test_函数、setup/teardown、基本assert的Python代码 |
| MVP-006 | 用户可上传日志文件（.log/.txt格式） | P0 | 支持文件上传，最大10MB |
| MVP-007 | Python预过滤机制：正则提取ERROR/Exception等关键字及其上下文 | P0 | 过滤后日志量≤原日志30%，保留异常堆栈完整性 |
| MVP-008 | AI分析过滤后的日志异常信息 | P0 | 识别错误堆栈、异常类型、出现频率 |
| MVP-009 | AI给出问题根因分析和排查建议 | P0 | 输出可能原因列表（按概率排序）和排查步骤 |
| MVP-010 | API Key配置：支持.env文件读取，Streamlit界面临时输入为辅 | P1 | 优先读取.env文件，无配置时可在界面输入 |
| MVP-011 | 简易本地Bug经验知识库：支持上传txt文件作为Context传入 | P1 | 支持读取本地txt文件，生成测试点时作为上下文 |
| MVP-012 | 界面美观，操作简单直观 | P1 | Streamlit单页应用，三步内完成核心操作 |

### 3.2 非功能需求

| 需求编号 | 需求描述 | 验收标准 |
|---------|---------|---------|
| NFR-001 | 响应时间 | 测试点生成：<30秒；测试用例生成：<45秒；日志分析：<30秒 |
| NFR-002 | 可用性 | 99%以上可用，无系统级故障 |
| NFR-003 | 安全性 | API Key仅本地使用，不上传任何服务器；支持环境变量配置 |
| NFR-004 | 兼容性 | 支持Chrome、Edge、Firefox主流浏览器 |
| NFR-005 | 日志处理能力 | 支持10MB日志文件，预过滤后Token数≤8000 |
| NFR-006 | 知识库支持 | 支持100KB以内的txt格式知识库文件 |

---

## 4. 功能模块设计

### 4.1 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                  Streamlit 单页应用 (Python)                │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ 测试点生成   │  │ 测试用例生成 │  │ 日志分析     │     │
│  │ 模块         │  │ 模块         │  │ 模块         │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                 │                 │              │
│         ▼                 ▼                 ▼              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ Bug知识库    │  │              │  │ Python       │     │
│  │ 加载模块     │  │              │  │ 预过滤引擎   │     │
│  └──────┬───────┘  └──────────────┘  └──────┬───────┘     │
│         │                                   │              │
│         └──────────────┬────────────────────┘              │
│                        ▼                                   │
│              ┌──────────────┐                              │
│              │ AI模型调用层 │                              │
│              │ (统一接口)   │                              │
│              └──────┬───────┘                              │
│                     │ API调用                              │
└─────────────────────┼─────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                   大模型服务 (外部)                          │
│    OpenAI GPT-4 / 阿里云通义千问 / 百度文心一言              │
└─────────────────────────────────────────────────────────────┘

配置文件：.env (API_KEY, MODEL_NAME, TEMPERATURE)
知识库文件：./knowledge/*.txt (本地Bug经验库)
```

### 4.2 功能模块详细设计

#### 模块一：测试点生成模块

**功能描述**：根据用户输入的需求描述或上传的PRD文档，结合可选的本地Bug经验知识库，AI自动生成结构化测试点文档。

**输入**：
- 需求描述文本（最大10000字符）
- PRD文档（.md/.txt格式，最大5MB）
- 可选：本地Bug经验知识库（txt格式，最大100KB）

**输出**：
- 结构化测试点Markdown文档，包含：
  - 需求概述
  - 功能测试点（正常流程）
  - 边界测试点（边界值、极限情况）
  - 异常测试点（错误输入、异常流程）
  - 性能测试点（响应时间、并发量）

**处理流程**：
```
用户输入 + 知识库 → 需求解析 → 场景分析 → 测试点生成 → 结果格式化 → 输出
```

#### 模块二：测试用例生成模块

**功能描述**：根据测试点文档，AI生成pytest测试用例代码骨架。

**输入**：
- 测试点文档（Markdown格式）
- 目标项目配置（模块名称、类名、方法名）

**输出**：
- pytest测试用例Python文件，包含：
  - import语句（pytest、被测模块）
  - 测试类/测试函数
  - setup_method/teardown_method（可选）
  - 测试用例方法（test_前缀）
  - 基本assert语句占位符

**处理流程**：
```
测试点文档 → 测试用例提取 → 代码结构生成 → pytest格式转换 → 输出
```

#### 模块三：日志分析模块（含Python预过滤机制）

**功能描述**：分析用户上传的日志文件，先通过Python正则预过滤提取异常信息，再提交给大模型分析，定位问题根因，给出排查建议。

**输入**：
- 日志文件（.log/.txt格式，最大10MB）

**输出**：
- 分析报告，包含：
  - 异常统计（类型、数量、频率）
  - 错误堆栈分析
  - 可能根因列表（按概率排序）
  - 排查步骤建议
  - 修复方案建议

**处理流程**：
```
日志文件 → Python正则预过滤（提取ERROR/Exception及上下文）→ 精简日志 → AI分析 → 根因分析 → 建议生成 → 输出
```

**预过滤机制详细设计**：

| 过滤规则 | 正则模式 | 上下文行数 | 说明 |
|---------|---------|-----------|------|
| ERROR级别 | `(?i)error` | 前后5行 | 匹配ERROR级别日志 |
| Exception | `(?i)exception|traceback` | 前后10行 | 匹配异常堆栈 |
| 关键错误词 | `(?i)fail|error|warn|critical|exception` | 前后3行 | 匹配常见错误关键词 |

**过滤策略**：
1. 按行读取日志文件
2. 匹配上述正则模式，标记异常行
3. 提取异常行及其上下文（前后N行）
4. 去重合并相同异常
5. 控制输出Token数≤8000

#### 模块四：配置管理模块

**功能描述**：管理AI模型的配置参数，支持.env文件读取和界面临时输入。

**输入**：
- 优先级1：.env环境变量文件
- 优先级2：Streamlit界面临时输入（当.env未配置时）

**配置项**：
- API_KEY：大模型API密钥
- MODEL_NAME：模型名称（如gpt-4, qwen-turbo等）
- TEMPERATURE：温度参数（0.1-1.0）
- MAX_TOKENS：最大输出Token数

**输出**：
- 配置加载/更新成功提示

**处理流程**：
```
启动应用 → 读取.env文件 → 配置生效 → 无配置时提示用户输入
```

#### 模块五：Bug经验知识库模块（加分项）

**功能描述**：支持加载本地txt格式的Bug经验知识库，在生成测试点时作为Context传入，实现简易RAG效果。

**输入**：
- 本地知识库文件（txt格式，最大100KB）

**存储格式示例**：
```
# 登录模块常见Bug
1. 用户名包含特殊字符导致SQL注入
2. 密码明文传输
3. 多次登录失败未锁定账户

# 支付模块常见Bug
1. 重复支付导致金额翻倍
2. 支付超时未回滚订单状态
3. 优惠券使用后退款未返还
```

**处理流程**：
```
加载知识库 → 提取关键词 → 与需求匹配 → 作为Context传入AI → 生成测试点
```

---

## 5. 后续可扩展功能

### 5.1 中期扩展（3-6个月）

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|-------|
| **测试用例管理** | 支持测试用例的增删改查、版本管理、标签分类 | P1 |
| **测试报告分析** | 上传测试报告，AI分析失败原因和改进建议 | P1 |
| **CI/CD集成** | 提供API接口，集成到Jenkins/GitLab CI流程 | P2 |
| **多语言支持** | 支持Java(JUnit)、JavaScript(Jest)等多语言测试框架 | P2 |
| **知识库增强** | 支持多文件、搜索、分类管理 | P2 |

### 5.2 长期扩展（6-12个月）

| 功能模块 | 功能描述 | 优先级 |
|---------|---------|-------|
| **智能测试推荐** | 根据代码变更自动推荐需要执行的测试用例 | P1 |
| **测试覆盖率分析** | 分析测试覆盖率，推荐未覆盖的测试点 | P1 |
| **知识图谱** | 构建测试知识库，沉淀测试经验和最佳实践 | P2 |
| **自动化测试执行** | 集成测试执行引擎，一键运行测试用例 | P2 |
| **移动端支持** | 开发移动端App，支持随时随地使用 | P3 |

---

## 6. 技术选型建议

### 6.1 核心技术栈

| 技术 | 选型 | 理由 |
|------|------|------|
| 框架 | Streamlit | 纯Python前端框架，快速搭建单页应用，无需前端知识 |
| 语言 | Python 3.10+ | 生态丰富，AI库支持好，测试框架成熟 |
| AI SDK | OpenAI API / LangChain | 支持多种大模型，封装完善 |
| 配置管理 | python-dotenv | 读取.env环境变量文件 |
| 日志处理 | re模块（正则表达式） | 轻量级预过滤，无需额外依赖 |

### 6.2 项目结构

```
aitestassistant/
├── main.py              # Streamlit主入口
├── .env                 # 环境变量配置（gitignore）
├── requirements.txt     # Python依赖
├── utils/
│   ├── ai_client.py     # AI模型调用封装
│   ├── config.py        # 配置管理
│   ├── logger_filter.py # 日志预过滤引擎
│   └── knowledge_base.py # 知识库加载模块
├── prompts/
│   ├── test_points.txt  # 测试点生成Prompt
│   ├── test_cases.txt   # 测试用例生成Prompt
│   └── log_analysis.txt # 日志分析Prompt
├── knowledge/
│   └── bug_experience.txt # 默认Bug经验知识库（示例）
└── outputs/             # 生成的测试文档和代码（可选）
```

### 6.3 部署方案

| 环境 | 方案 | 说明 |
|------|------|------|
| 开发环境 | Localhost | 直接运行 `streamlit run main.py` |
| 生产环境 | Streamlit Community Cloud / Docker | 一键部署或容器化部署 |

---

## 7. 项目进度规划（个人开发者3周版）

### 第1周：基础框架搭建 + AI集成

| 任务 | 时间 | 交付物 |
|------|------|--------|
| Streamlit项目初始化 | 0.5天 | `main.py`入口文件，基础页面结构 |
| 环境配置模块 | 0.5天 | `.env`配置、`python-dotenv`集成 |
| AI模型调用封装 | 2天 | 支持OpenAI模型调用，统一接口 |
| 基础UI页面开发 | 2天 | 三大功能页面框架，导航切换 |

### 第2周：核心功能开发 - 测试点生成 + 测试用例生成

| 任务 | 时间 | 交付物 |
|------|------|--------|
| 需求输入模块 | 0.5天 | 文本输入、文件上传组件 |
| Bug经验知识库模块 | 0.5天 | 本地txt文件加载，Context拼接 |
| AI测试点生成 | 2天 | Prompt优化，结构化输出 |
| 测试点展示导出 | 0.5天 | Markdown渲染，文件导出 |
| 测试用例生成 | 1.5天 | 测试点转pytest代码 |

### 第3周：核心功能开发 - 日志分析 + 完善上线

| 任务 | 时间 | 交付物 |
|------|------|--------|
| Python预过滤引擎 | 1.5天 | 正则提取ERROR/Exception及上下文 |
| 日志上传模块 | 0.5天 | 文件上传、大小校验 |
| AI日志分析 | 1.5天 | 异常识别、根因分析 |
| 界面优化 | 0.5天 | UI美化、交互优化 |
| 测试调试 | 1天 | 功能测试、Bug修复 |
| 部署上线 | 0.5天 | Streamlit Cloud部署 |

---

## 8. 风险评估与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| AI模型调用失败 | 中 | 高 | 增加重试机制，支持多模型切换 |
| 日志Token溢出 | 中 | 高 | 严格执行预过滤机制，控制Token数≤8000 |
| 生成质量不稳定 | 中 | 高 | 优化prompt工程，增加知识库Context |
| API费用成本 | 低 | 中 | 本地缓存结果，减少重复调用 |
| 隐私安全问题 | 低 | 高 | API Key仅本地使用，不上传任何服务器 |

---

## 9. 验收标准

### 9.1 功能验收

| 功能 | 验收标准 |
|------|---------|
| 测试点生成 | 输入1000字需求描述，30秒内生成包含功能/边界/异常测试点的文档 |
| 知识库集成 | 上传100KB知识库文件，生成测试点时能引用知识库内容 |
| 测试用例生成 | 输入10个测试点，45秒内生成完整的pytest测试用例代码 |
| 日志预过滤 | 上传10MB日志文件，过滤后日志量≤30%，保留异常堆栈 |
| 日志分析 | 上传5MB日志文件，30秒内输出异常分析报告和排查建议 |
| 配置管理 | .env文件配置生效，无配置时界面可临时输入 |

### 9.2 性能验收

| 指标 | 验收标准 |
|------|---------|
| 页面加载时间 | <2秒 |
| AI响应时间 | <45秒 |
| 日志预过滤耗时 | <5秒（10MB文件） |

### 9.3 兼容性验收

| 指标 | 验收标准 |
|------|---------|
| 浏览器兼容性 | Chrome、Edge、Firefox最新版正常运行 |
| 文件格式支持 | .md、.txt、.log格式正常上传解析 |

---

## 10. 附录

### 10.1 测试点文档输出格式示例

```markdown
# 测试点文档

## 1. 需求概述
[需求描述摘要]

## 2. 功能测试点

| 编号 | 测试场景 | 前置条件 | 测试步骤 | 预期结果 |
|------|---------|---------|---------|---------|
| F001 | 用户正常登录 | 用户已注册 | 1. 输入用户名密码 2. 点击登录 | 登录成功，跳转首页 |

## 3. 边界测试点

| 编号 | 测试场景 | 测试数据 | 预期结果 |
|------|---------|---------|---------|
| B001 | 用户名长度边界 | 用户名=1字符 | 提示用户名长度不足 |

## 4. 异常测试点

| 编号 | 测试场景 | 测试数据 | 预期结果 |
|------|---------|---------|---------|
| E001 | 用户名不存在 | 不存在的用户名 | 提示用户不存在 |

## 5. 性能测试点

| 编号 | 测试场景 | 预期指标 |
|------|---------|---------|
| P001 | 登录响应时间 | <1秒 |
```

### 10.2 pytest测试用例输出格式示例

```python
import pytest
from your_module import UserService

class TestUserLogin:
    def setup_method(self):
        self.user_service = UserService()
    
    def teardown_method(self):
        pass
    
    def test_login_success(self):
        """测试用户正常登录"""
        result = self.user_service.login("test_user", "password")
        assert result["success"] == True
        assert result["message"] == "登录成功"
    
    def test_login_with_none_username(self):
        """测试用户名为空"""
        with pytest.raises(ValueError):
            self.user_service.login(None, "password")
```

### 10.3 日志分析报告输出格式示例

```markdown
# 日志分析报告

## 1. 异常统计

| 异常类型 | 出现次数 | 占比 |
|---------|---------|------|
| NullPointerException | 25 | 62.5% |
| TimeoutException | 10 | 25% |
| ConnectionException | 5 | 12.5% |

## 2. 根因分析

**最可能原因**：数据库连接池耗尽
- 证据：ConnectionException集中在数据库操作时段
- 概率：80%

**次要可能原因**：代码空指针检查缺失
- 证据：NullPointerException堆栈指向UserService.java:123
- 概率：60%

## 3. 排查建议

1. 检查数据库连接池配置（最大连接数、超时时间）
2. 查看UserService.java第123行代码，增加空值判断
3. 监控数据库连接状态，确认是否存在连接泄漏

## 4. 修复方案

- 方案一：增加连接池最大连接数，优化连接复用策略
- 方案二：在空指针位置增加防御性编程
```

### 10.4 .env配置文件格式示例

```env
# AI模型配置
API_KEY=your-api-key-here
MODEL_NAME=gpt-4o
TEMPERATURE=0.7
MAX_TOKENS=4096

# 知识库配置
KNOWLEDGE_BASE_PATH=./knowledge/bug_experience.txt
```

### 10.5 Bug经验知识库格式示例

```
# 登录模块常见Bug
1. 用户名包含特殊字符导致SQL注入
2. 密码明文传输
3. 多次登录失败未锁定账户
4. 验证码过期未刷新

# 支付模块常见Bug
1. 重复支付导致金额翻倍
2. 支付超时未回滚订单状态
3. 优惠券使用后退款未返还
4. 金额计算精度丢失

# 订单模块常见Bug
1. 库存超卖
2. 订单状态流转错误
3. 删除订单后数据残留
```